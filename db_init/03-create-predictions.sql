CREATE TABLE IF NOT EXISTS "ohlcv_1m_predictions" (
  "SYMBOL_VALUE"       TEXT NOT NULL,
  "WINDOW_START"       BIGINT NOT NULL,           -- epoch ms
  "PREDICTED_VOLUME"   DOUBLE PRECISION NOT NULL,
  "MODEL_VERSION"      TEXT NOT NULL,
  "FEATURES_LAG_MINUTES" INTEGER[] NOT NULL,
  "CREATED_AT"         BIGINT NOT NULL,
  PRIMARY KEY ("SYMBOL_VALUE","WINDOW_START")
);
